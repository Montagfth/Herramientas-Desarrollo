# PostgreSQL
POSTGRES_DB=pharmacydb
POSTGRES_USER=pharmacy
POSTGRES_PASSWORD=pharmacy123

# JWT (mínimo 32 bytes, base64 o texto plano)
JWT_SECRET=ClaveSecretaSuperSeguraParaFarmacia2026Minimo32Bytes!
JWT_EXPIRATION=86400000

# CORS - orígenes permitidos (coma-separados, sin espacios o con espacios trimmed)
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:80,http://localhost:5173

# Frontend (solo build-time)
# VITE_API_BASE_URL=/api
